# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Settings / validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Database (SQLAlchemy 2.x + psycopg v3 driver, with pgvector support)
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0
pgvector>=0.3.0

# Async HTTP client — used for both provider API calls and Node API callbacks.
httpx>=0.27.0

# Environment
python-dotenv>=1.0.1
